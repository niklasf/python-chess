language: python
python:
 - "2.7"
 - "3.4"
install:
 - pip install coveralls --use-mirrors
script:
 - coverage run --source chess test.py
 - coverage run --source chess --append -m doctest README.rst
 - coverage run --source chess --append perft.py
after_success:
 - coveralls
